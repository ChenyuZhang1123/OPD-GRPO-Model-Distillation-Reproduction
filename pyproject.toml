[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "opd"
version = "0.1.0"
description = "On-Policy Distillation for math reasoning"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
